name: CI

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install -r dev-requirements.txt

      - name: Lint
        run: ruff check computer_use_demo tests scripts

      - name: Test
        run: python -B -m pytest -q tests/test_api_app.py tests/test_config.py tests/test_db.py tests/test_orchestrator_sessions.py tests/test_worker_api.py tests/test_worker_manager.py
